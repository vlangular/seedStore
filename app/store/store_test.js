'use strict';

describe('seedApp.store module', function() {

  beforeEach(module('seedApp.store'));

  describe('store controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var StoreCtrl = $controller('StoreCtrl');
      expect(StoreCtrl).toBeDefined();
    }));

  });
});
